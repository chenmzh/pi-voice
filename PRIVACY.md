# 隐私与发布边界

## 禁止进入仓库的内容

- 真实参考音色、录音、合成音频及逐字稿。
- 模型权重、Python 环境和第三方后端安装目录。
- 用户的 `local-voice.json`、Pi settings/auth、会话记录、环境变量文件和凭证。
- 本机诊断日志、终端录屏、验证产物、个人绝对路径和源文件备份。

Git 测试中的声音名称是通用虚构标识。占位音频在测试临时目录中生成，不含真实声音，也不会提交。涉及导入解码的单元测试只生成数学正弦信号。

本仓库初始提交从明确的代码文件清单建立，不导入原工作区的 Git 历史。

## 本地运行

录音通常只在内存中；播放使用私有临时 WAV，并在正常完成／取消后清理。异常崩溃可能留下临时文件。仅在你明确执行音色克隆／导入时，才持久保存参考音频与逐字稿；文件以私有权限创建，已有同名文件拒绝覆盖。

DSH 音色目录仅用于发现和读取参考音频，不修改其配置或覆盖文件。不同 Pi 实例可能共享配置文件／音色文件，但不共享已加载的 GPU 模型实例。

Pi Voice 本身不提供云端语音接口。后端由用户自行提供和管理，其依赖和安全性也需自行审查。转写文本由你确认发送后，会随正常 Pi 对话传给当前对话模型；若使用云端对话模型，这一步并不离线。

## 上传前检查

1. 仔细检查 `git diff --cached` 和 `git ls-files`，只暂存计划发布的代码。
2. 执行 `npm run check:privacy`，检查**暂存版本**，而非仅工作目录。
3. 检查 `npm pack --dry-run --json` 的文件清单。
4. 不使用 `git add -f` 绕过音频、逐字稿、配置或模型的忽略规则。

检查脚本采用保守文件白名单并拦截常见凭证、真实用户主目录路径和二进制内容；不是完整的秘密检测器。新增文件类型需要明确审阅并更新白名单，不能仅凭自动检查通过就认定可以公开。

若私密内容已提交，后续删除不会清除 Git 历史。应在上传前重新建立干净提交；已经泄露的凭证应撤销并轮换，不要把改成私有仓库当作补救完成。
